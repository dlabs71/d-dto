describe("first test", () => {

    it("first test", () => {
        let simpleObj = {a: "123"};
        let promise = new Promise(resolve=>resolve("asd"));
        console.log(promise.name);
    });
});